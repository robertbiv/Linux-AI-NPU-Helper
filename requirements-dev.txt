# Development dependencies (docs, testing, linting)
-r requirements.txt

# Testing
pytest>=8.3.5
pytest-cov>=6.0.0
pytest-mock>=3.14.0
pytest-qt>=4.4.0

# Documentation — MkDocs + Material (current gold standard)
mkdocs>=1.6.1
mkdocs-material>=9.6.0
mkdocstrings[python]>=1.0.0

# Optional GUI (not required for tests or docs)
# PyQt5>=5.15.0
